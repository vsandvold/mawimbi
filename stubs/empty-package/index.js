// Intentionally empty. If this ever gets imported at runtime, the importer
// expected a real Node-only package (see package.json overrides) — which
// would mean browser code took a Node-only path and should fail loudly there.
module.exports = {};
