import { ProjectAction, ProjectState } from './projectReducer';

const useProjectEffects = (
  state: ProjectState,
  dispatch: React.Dispatch<ProjectAction>
) => {};

export default useProjectEffects;
